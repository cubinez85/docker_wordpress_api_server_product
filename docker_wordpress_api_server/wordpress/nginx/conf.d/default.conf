server {
    listen 80;
    listen [::]:80;
    server_name wordpress.cubinez.ru;  # Замените на ваш домен, если есть

    root /var/www/html;
    index index.php index.html;

    # Лимит размера загружаемых файлов
    client_max_body_size 64M;

    # Основной location
    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    # Обработка PHP через FastCGI к контейнеру WordPress
    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass wordpress:9000;   # Имя контейнера WordPress и порт PHP-FPM
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;

        # Оптимизация
        fastcgi_buffer_size 128k;
        fastcgi_buffers 4 256k;
        fastcgi_busy_buffers_size 256k;
    }

    # Запрет доступа к скрытым файлам (.htaccess, .env и т.д.)
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # Статические файлы с кэшированием
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        access_log off;
        add_header Cache-Control "public, immutable";
    }
}
